<%- include('_top', { appName: 'ClientPortal API', pageTitle: 'API Key', theme: 'headers', tag: '/labs/cors-wildcard-credentials' }) %>
<div class="lab-card">
  <h1>Your API key</h1>
  <p>Key: <code><%= apiKeyPreview %></code> (truncated for display -- the full key never appears in the UI, only via the API)</p>
</div>

<%- include('_bottom') %>
